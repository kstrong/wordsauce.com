        <h3>Upcoming Shows</h3>
        <% if @shows.count > 0 %>
        <table>
          <thead>
            <tr>
              <th>Date</th><th>Location</th><th>Venue</th><!--<th>Links</th>-->
            </tr>
          </thead>
          <tbody>
          <% @shows.first(10).each do |show| %>
            <tr>
              <td><%= show[:date] %></td>
              <td>
                <%= show[:location] %>
                <% if show.key?(:prtm) && show[:prtm] == true %>
                  <span class="prtm-indicator" title="Punk Rock Time Machine">*</span>
                <% end %>
              </td>
              <td>
                <% if show.key?(:links) && show[:links].count > 0 %>
                  <a href="<%= show[:links].first %>" target="_blank"><%= show[:venue] %></a>
                <% else %>
                  <%= show[:venue] %>
                <% end %>
                <% if show.key?(:prtm) && show[:prtm] == true %>
                  <span class="prtm-indicator" title="Punk Rock Time Machine">*</span>
                <% end %>
              </td>
            </tr>
          <% end %>
          </tbody>
        </table>
        <div class="row">
          <div class="large-6 medium-6 columns">
            <a href="/tour" class="button radius small">More Tour Dates &raquo;</a>
          </div>
          <% if (@shows.select { |show| show.key?(:prtm) && show[:prtm] = true }).count > 0 %>
          <div class="large-6 medium-6 columns">
            <span class="prtm-indicator">*</span> = Punk Rock Time Machine
          </div>
          <% end %>
        </div>
        <% else %>
        <div class="row">
          <div class="large-12 columns">
            <p>
              Interested in booking Wordsauce? <a href="/contact">Contact Us</a>
            </p>
          </div>
        </div>
        <div class="row">
          <div class="large-6 medium-6 columns">
            <a href="/tour" class="button radius small">Past Tour Dates &raquo;</a>
          </div>
        </div>
        <% end %>